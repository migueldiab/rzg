-- -----------------------------------------------------
-- Table `fecha_etapa_carrera`
-- -----------------------------------------------------

ALTER  TABLE `fecha_etapa_carrera` ADD COLUMN (
  `estado` VARCHAR(1) NULL DEFAULT 'I' );
